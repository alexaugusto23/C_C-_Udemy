#include <stdio.h> // entrada e saida de dados
#include<stdlib.h> // simula prompt dos
#include<stdbool.h>

void main()
{
  char letra = 'x';
  
  if(letra == 'x'){printf("\nA letra eh X\n");}
  printf("\nValor tabela ASSCII: %d\n", letra);
  if(letra == 120){printf("\nA letra eh X\n");}
 
 
printf("\n");	
system ("pause");
}